---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { articles } from "@/data/articles";
import { externalGames } from "@/data/externalGames";
import { playFocusAreas } from "@/data/gameData";
import { siteConfig } from "@/data/siteConfig";
import { brandAccentClass, getFocusAccentClass } from "@/data/accentClasses";

const recentPosts = articles.slice(0, 3);
const mainGame = {
  href: "/play/",
  title: "Arrows Go Online",
  shortTitle: "Arrows Go",
  category: "Now playing",
  thumbnail: "/game-thumbs/arrows-go.svg",
  accent: "#12b886",
  accentClass: brandAccentClass,
};
const homeBentoGames = [
  mainGame,
  ...externalGames.map((game) => ({
    href: `/games/${game.slug}/`,
    title: game.title,
    shortTitle: game.shortTitle,
    category: game.category,
    thumbnail: game.thumbnail,
    accent: game.accent,
    accentClass: game.accentClass,
  })),
].slice(0, 8);
const homeBentoLeft = homeBentoGames.slice(0, 4);
const homeBentoRight = homeBentoGames.slice(4, 8);

const faqItems = [
  ["What is Arrows Go?", "Arrows Go is a free browser arcade game where you guide a moving arrow by changing direction and avoiding hazards."],
  ["How do you play Arrows Go online?", "Open the play page, watch the arrow path, and time each direction change before the next hazard closes in."],
  ["Is Arrows Go free?", "Yes. Arrows Go is free to play in the browser with no download, no account, and no app install required."],
  ["Does Arrows Go work on mobile?", "Yes. The embedded game is designed for browser play and supports touch or mouse input where the player allows it."],
  ["Why is the iframe loaded through /game/index.html?", "The local wrapper keeps the main Astro site route stable while loading the hosted HTML5 game inside the playable frame."],
];

const homeStructuredData = [
  {
    "@context": "https://schema.org",
    "@type": "VideoGame",
    name: "Arrows Go",
    alternateName: ["Arrows Go Online", "ArrowsGo", "Arrow arcade game"],
    url: `${siteConfig.siteUrl}/`,
    image: new URL(siteConfig.defaultOgImage, siteConfig.siteUrl).href,
    description: siteConfig.description,
    applicationCategory: "Game",
    gamePlatform: ["Web browser", "Desktop", "Mobile", "Tablet"],
    genre: ["Arcade", "Timing", "Reflex", "Arrow game"],
    operatingSystem: "Any modern browser",
    offers: {
      "@type": "Offer",
      price: "0",
      priceCurrency: "USD",
      availability: "https://schema.org/InStock",
    },
    publisher: {
      "@type": "Organization",
      name: siteConfig.publisherName,
      url: siteConfig.siteUrl,
    },
  },
  {
    "@context": "https://schema.org",
    "@type": "FAQPage",
    mainEntity: faqItems.map(([question, answer]) => ({
      "@type": "Question",
      name: question,
      acceptedAnswer: {
        "@type": "Answer",
        text: answer,
      },
    })),
  },
];
---

<BaseLayout title={siteConfig.name} description={siteConfig.description} structuredData={homeStructuredData}>
  <section class="hero-play">
    <div class="home-game-lobby">
      <div class="game-bento-grid game-bento-grid-side" aria-label="Games you can play">
        {homeBentoLeft.map((game, index) => (
          <a class:list={["game-bento-card", game.accentClass, { "game-bento-card-featured": index === 0 }]} href={game.href}>
            <img src={game.thumbnail} alt={`${game.title} thumbnail`} width="640" height="360" loading="lazy" />
            <div>
              <span>{game.category}</span>
              <strong>{game.shortTitle}</strong>
            </div>
          </a>
        ))}
      </div>
      <div class="hero-game">
        <iframe src="/game/index.html" title="Play Arrows Go" loading="eager"></iframe>
      </div>
      <div class="game-bento-grid game-bento-grid-side" aria-label="More games">
        {homeBentoRight.map((game, index) => (
          <a class:list={["game-bento-card", game.accentClass, { "game-bento-card-featured": index === 0 }]} href={game.href}>
            <img src={game.thumbnail} alt={`${game.title} thumbnail`} width="640" height="360" loading="lazy" />
            <div>
              <span>{game.category}</span>
              <strong>{game.shortTitle}</strong>
            </div>
          </a>
        ))}
      </div>
    </div>
    <div class="hero-copy">
      <div class="hero-copy-main">
        <p class="eyebrow">Instant play</p>
        <h1>Arrows Go Online</h1>
        <p>Play Arrows Go free in your browser. Change direction at the right moment, avoid hazards, and keep the arrow moving with no download and no account.</p>
        <a class="text-link hero-note" href="/how-to-play/">Learn the rules</a>
      </div>
      <div class="hero-play-panel">
        <div class="hero-panel-top">
          <span>Ready in browser</span>
          <strong>Start a quick run.</strong>
        </div>
        <div class="hero-actions">
          <a class="primary-action" href="/play/"><span class="play-triangle"></span>Play Full Screen</a>
          <a class="secondary-action" href="/how-to-play/">How To Play</a>
        </div>
        <dl class="stat-row">
          <div><dt>0</dt><dd>Downloads</dd></div>
          <div><dt>4</dt><dd>Core Skills</dd></div>
          <div><dt>{articles.length}</dt><dd>Guides</dd></div>
        </dl>
      </div>
    </div>
  </section>

  <section class="home-section home-search-intent">
    <div class="home-section-head split-head">
      <div>
        <p class="eyebrow">Arrows Go guide</p>
        <h2>Play the arrow arcade timing game.</h2>
      </div>
      <p>Arrows Go turns one simple idea into a tight browser challenge: read the route, time the direction change, and survive the next hazard.</p>
    </div>
    <div class="process-strip">
      <article>
        <span>Free</span>
        <h3>Browser play</h3>
        <p>Open <a href="/play/">Arrows Go online</a> and start instantly. The game runs in the browser with no app store, installer, or sign-up flow.</p>
      </article>
      <article>
        <span>Turn</span>
        <h3>Direction timing</h3>
        <p>Watch the arrow path and change direction before the next hazard closes the lane.</p>
      </article>
      <article>
        <span>Focus</span>
        <h3>Fast restarts</h3>
        <p>Each attempt is short enough to learn from. Notice the late turn, restart, and make the next decision cleaner.</p>
      </article>
      <article>
        <span>Learn</span>
        <h3>Helpful paths</h3>
        <p>Use the <a href="/how-to-play/">how to play guide</a>, read <a href="/strategy/">strategy tips</a>, or explore <a href="/games/">related games</a>.</p>
      </article>
    </div>
  </section>

  <section class="home-section home-authority">
    <div class="authority-panel">
      <div class="home-section-head">
        <p class="eyebrow">Arcade context</p>
        <h3>Built for quick directional decisions.</h3>
        <p>
          Keep <a class="site-identity-link" href="/"><strong>www.arrowsgo.com</strong></a> as your route back to the free browser game, then explore the classic arcade ideas behind timing and reaction play.
        </p>
      </div>
      <div class="authority-links" aria-label="External arcade references">
        <a href="https://en.wikipedia.org/wiki/Arcade_video_game" target="_blank" rel="noopener noreferrer">
          <span>Wikipedia</span>
          <strong>Arcade game history</strong>
          <small>How short-session action games became a lasting format.</small>
        </a>
        <a href="https://en.wikipedia.org/wiki/Reaction_time" target="_blank" rel="noopener noreferrer">
          <span>Wikipedia</span>
          <strong>Reaction time</strong>
          <small>The attention and response concept behind reflex challenges.</small>
        </a>
        <a href="https://www.youtube.com/results?search_query=arrow+arcade+gameplay" target="_blank" rel="noopener noreferrer">
          <span>YouTube</span>
          <strong>Gameplay videos</strong>
          <small>Watch arrow, timing, and reflex game examples.</small>
        </a>
      </div>
    </div>
  </section>

  <section class="home-section home-worlds">
    <div class="home-section-head">
      <p class="eyebrow">Core loop</p>
      <h3>Four skills that shape every run.</h3>
      <p>Short attempts make each skill easy to practice one at a time.</p>
    </div>
    <div class="world-showcase">
      <a class:list={["world-feature", getFocusAccentClass(playFocusAreas[0].name)]} href="/strategy/">
        <img src="/game-thumbs/arrows-go.svg" alt="Arrows Go direction control preview" width="640" height="360" loading="lazy" />
        <div>
          <span>{playFocusAreas[0].name}</span>
          <strong>Turn before the lane closes.</strong>
          <small>{playFocusAreas[0].description}</small>
        </div>
      </a>
      <div class="world-stack">
        {playFocusAreas.slice(1).map((focus) => (
          <a class:list={["world-card", getFocusAccentClass(focus.name)]} href="/strategy/">
            <img src="/game-thumbs/arrows-go.svg" alt={`${focus.name} Arrows Go preview`} width="640" height="360" loading="lazy" />
            <div>
              <span>{focus.name}</span>
              <small>{focus.description}</small>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <section class="home-section home-process">
    <div class="home-section-head split-head">
      <div>
        <p class="eyebrow">How it works</p>
        <h3>Read. Turn. Survive. Retry.</h3>
      </div>
      <p>Four steps from first input to a cleaner run.</p>
    </div>
    <div class="process-strip">
      <article><span>01</span><h3>Read</h3><p>Scan the next lane before the arrow reaches danger.</p></article>
      <article><span>02</span><h3>Turn</h3><p>Change direction with one deliberate input at the right moment.</p></article>
      <article><span>03</span><h3>Dodge</h3><p>Avoid hazards and keep enough space for the next decision.</p></article>
      <article><span>04</span><h3>Retry</h3><p>Use each failed run to tune the next timing window.</p></article>
    </div>
  </section>

  <section class="home-section home-blog">
    <div class="blog-editorial">
      <div class="home-section-head">
        <p class="eyebrow">From the blog</p>
        <h3>Sharper arcade timing.</h3>
        <p>Short reads for cleaner turns, better focus, and mobile play.</p>
        <a class="text-link" href="/blog/">Browse all articles</a>
      </div>
      <div class="blog-board">
        <a class="editorial-card editorial-card-featured" href={`/blog/${recentPosts[0].slug}/`}>
          <span>{recentPosts[0].category}</span>
          <h3>{recentPosts[0].title}</h3>
          <p>{recentPosts[0].description}</p>
          <small>{recentPosts[0].readTime}</small>
        </a>
        <div class="article-rail">
          {recentPosts.slice(1).map((post) => (
            <a class="editorial-card editorial-card-row" href={`/blog/${post.slug}/`}>
              <span>{post.category}</span>
              <h3>{post.title}</h3>
              <small>{post.readTime}</small>
            </a>
          ))}
        </div>
      </div>
    </div>
  </section>

  <section class="home-section home-faq">
    <div class="faq-layout">
      <div class="faq-cardlet">
        <p class="eyebrow">FAQ</p>
        <h3>Quick answers.</h3>
        <p>Need the short version before playing?</p>
      </div>
      <div class="faq-list" data-faq-list>
        {faqItems.map(([question, answer]) => (
          <details>
            <summary>{question}</summary>
            <p>{answer}</p>
          </details>
        ))}
      </div>
    </div>
  </section>

  <section class="home-cta">
    <div>
      <p class="eyebrow">Ready?</p>
      <h3>Open Arrows Go and start a run.</h3>
    </div>
    <a class="primary-action" href="/play/"><span class="play-triangle"></span>Play Arrows Go</a>
  </section>

  <script is:inline>
    (() => {
      document.querySelectorAll("[data-faq-list]").forEach((list) => {
        list.querySelectorAll("details").forEach((item) => {
          item.addEventListener("toggle", () => {
            if (!item.open) return;
            list.querySelectorAll("details[open]").forEach((other) => {
              if (other !== item) other.open = false;
            });
          });
        });
      });
    })();
  </script>
</BaseLayout>
